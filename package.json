{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.ts",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "cd build && NODE_ENV=production node --env-file ../.prod.env index.js",
    "dev": "cd build && NODE_ENV=development nodemon --env-file ../.dev.env index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "sha3": "^2.1.4"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^25.0.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
